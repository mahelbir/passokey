services:
  passokey:
    container_name: passokey
    build:
      context: ./Application
      dockerfile: Dockerfile
    ports:
      - "4050:4050"
    volumes:
      - ./data:/app/App_Data
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
